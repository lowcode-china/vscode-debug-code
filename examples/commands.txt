// debug python
$cwd=${workspaceFolder}/python-project; $description="debug here"; DEBUG=ON; python main.py --help
$cwd=${workspaceFolder}/python-project; DEBUG=ON; PYTHONPATH="lib1:lib2"; python -m a.main --help
$cwd=${workspaceFolder}/python-project; python -m flask --app flask_server run
python -m pytest -s ${workspaceFolder}/python-project/tests.py::test_a
python -m pytest -s ${workspaceFolder}/python-project/tests.py

// debug golang
go run ${workspaceFolder}/go-project/main.go --help
$program=${workspaceFolder}/go-project; go test "" -test.v
$program=${workspaceFolder}/go-project; go test "" -test.v -test.run=TestHelloWorld

// debug node
node ${workspaceFolder}/node-project/main.js
node ${workspaceFolder}/node-project/main.ts

// debug rust
$justMyCode=true; DEBUG=ON; cargo run --manifest-path ${workspaceFolder}/rust-project/Cargo.toml --

// others
git -v
cd ./vite-project; pnpm dev